{
    "epochs": 100,
    "use_val": true,
    "learning_rate": 0.0001,
    "batch_size": 32,
    "architecture": "test4",
	"_comment": "Full version, with 30 partitions and early stopping, but the early stopping uses accuracy instead of loss"
}